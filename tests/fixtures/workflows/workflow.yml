on:
  activitypub:
    host: toot.io
    user: grislyeye

jobs:
  print:
    name: Print
    runs-on: ubuntu-latest
    steps:
      - name: Print Outputs
        env:
          title: ${{on.example.outputs.title}}
        run: |
          echo title: $title
